[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "c4-diagram"
version = "0.1.0"
description = "C4 diagram design knowledge service"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Salim" }]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/c4"]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
