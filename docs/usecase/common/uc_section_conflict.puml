' ============================================================
' Section: uc_section_conflict.puml
' Group:   conflicts_domain (domain · not peer) + findings review
' ResolveConflict is a domain service (class / package domain).
' ReviewConflictArtifact is findings_application (actor-facing).
' ============================================================

rectangle "$PKG_CONFLICTS_DOMAIN" as GRP_CON $UC_PKG_DOMAIN {
  usecase "$UC_NAME_RESOLVE_CONFLICT" as UC_CON_RESOLVE $CA_FILL_DOMAIN
}

rectangle "$PKG_FINDINGS_APPLICATION" as GRP_FIND_CON $UC_PKG_APPLICATION {
  usecase "$UC_NAME_REVIEW_CONFLICT_ARTIFACT" as UC_FIND_CONFLICT $CA_FILL_APPLICATION
}
