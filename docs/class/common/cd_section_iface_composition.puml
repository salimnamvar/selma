' Section: cd_section_iface_composition.puml

package "composition_root" as WIRE #F5EEF8/8E44AD {

  class "**ApplicationContainer**" as ApplicationContainer <<Composition>> #F5EEF8/8E44AD {
    + build(): ApplicationContainer
    + rest_app(): RestApplication
    + cli_app(): CliApplication
    + tui_app(): TuiApplication
  }

  class "**PortBindings**" as PortBindings <<Composition>> #F5EEF8/8E44AD {
    + bind_directive_ports()
    + bind_compile_ports()
    + bind_inspection_ports()
    + bind_finding_ports()
    + bind_artifact_ports()
    + bind_gate(DenialAuditPort)
  }

  class "**OutboxDrainScheduler**" as OutboxDrainScheduler <<Composition>> #F5EEF8/8E44AD {
    + start()
    + tick(): int
    --
    <<invokes>> DrainOutbox
  }
}

