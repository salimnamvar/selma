{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://contracts/evaluative-rule-schema.json",
  "title": "Universal Evaluative Rule Schema",
  "description": "Machine, agent, and human-readable data contract. Completely context-free. Applies to international laws, corporate policies, engineering standards, scientific protocols, and AI governance frameworks.",
  "type": "object",
  "required": ["version", "jurisdiction_scope", "directives"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semver of the contract format."
    },
    "jurisdiction_scope": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "description": "Abstract labels defining where this schema is law (e.g., EU, Engineering-Dept, Solar-System)."
    },
    "semantic_glossary": {
      "type": "object",
      "description": "Global key-value store for terms used across directives.",
      "additionalProperties": { "type": "string" }
    },
    "editorial_ref": {
      "type": "object",
      "description": "Reference to the human-readable editorial doctrine.",
      "required": ["document"],
      "properties": {
        "document": {
          "type": "string",
          "description": "URI or logical identifier of the editorial doctrine document (e.g., urn:doctrine:un-climate-v1)."
        }
      },
      "additionalProperties": false
    },
    "limits": {
      "type": "object",
      "additionalProperties": { "type": ["integer", "number", "string", "boolean"] },
      "description": "Named thresholds referenced by directives (DRY — single source of truth)."
    },
    "vocabulary": {
      "type": "object",
      "description": "Shared word lists and term registries used by directives.",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },
    "extensions": {
      "type": "object",
      "description": "Domain-specific extension data. Unconstrained.",
      "additionalProperties": true
    },
    "directives": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/directive" },
      "description": "Directive definitions — specific obligations and flexible standards."
    }
  },
  "additionalProperties": false,
  "definitions": {
    "directive": {
      "type": "object",
      "required": ["id", "kind", "anchor_ref", "evaluator_type", "message", "enforcement_weight"],
      "description": "A single directive — either a specific obligation or a flexible standard.",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[A-Z]{2,5}-\\d{4}$",
          "description": "Stable directive ID. Prefix matches jurisdiction or domain, e.g. XX-001, XX-002."
        },
        "kind": {
          "type": "string",
          "enum": ["obligation", "prohibition", "standard", "permission"],
          "description": "The nature of this directive: obligation (must do), prohibition (must not do), standard (principle-based), permission (may do)."
        },
        "anchor_ref": {
          "type": "string",
          "description": "URI or semantic path linking directly to the Editorial Doctrine section (e.g., urn:doctrine:section:/principles/fairness)."
        },
        "evaluator_type": {
          "type": "string",
          "enum": ["deterministic_algorithm", "statistical_model", "human_reviewer", "llm_judge", "external_oracle"],
          "description": "Defines the nature of the evaluator — who or what measures compliance."
        },
        "message": {
          "type": "string",
          "minLength": 5,
          "description": "Violation message template or judgment output."
        },
        "conditions": {
          "type": "object",
          "description": "The exclusive gatekeeper. If conditions are not met, this directive is inert.",
          "properties": {
            "jurisdictions": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Active if jurisdiction matches any of these labels."
            },
            "lifecycle_state": {
              "type": "string",
              "enum": ["draft", "active", "sunset"],
              "description": "Active if in this lifecycle state."
            },
            "unless": {
              "type": "object",
              "description": "Explicit skip logic (e.g., {'income_bracket': 'low'}). If any condition matches, directive is inert.",
              "additionalProperties": true
            }
          },
          "additionalProperties": true
        },
        "enforcement_weight": {
          "type": "string",
          "enum": ["absolute_veto", "rebuttable_presumption", "best_effort", "informative"],
          "description": "Legal/Operational severity detached from code severity. absolute_veto = zero tolerance; informative = best practice."
        },
        "reasoning": {
          "type": "string",
          "description": "Why this directive exists — the doctrinal rationale."
        },
        "fix": {
          "type": "string",
          "description": "Actionable fix suggestion or remediation path."
        },
        "params": {
          "type": "object",
          "description": "Unconstrained key-values. Accepts tax rates, regex, word counts, geopolitical zones, financial thresholds, or any domain-specific parameters.",
          "additionalProperties": true
        },
        "semantic_tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Keywords for semantic matching with the editorial prose (e.g., ['privacy', 'data-handling'])."
        },
        "interpretation": {
          "type": "object",
          "description": "Guidance for applying judgment. Use with kind=standard.",
          "properties": {
            "factors": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Relevant considerations when interpreting this standard."
            },
            "examples": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "context": { "type": "string" },
                  "outcome": { "type": "string" }
                }
              },
              "description": "Illustrative scenarios showing how the standard applies."
            },
            "anti_patterns": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Common misinterpretations to avoid."
            }
          },
          "additionalProperties": false
        }
      },
      "allOf": [
        {
          "if": { "properties": { "kind": { "const": "obligation" } } },
          "then": { "required": ["params"] }
        },
        {
          "if": { "properties": { "kind": { "const": "prohibition" } } },
          "then": { "required": ["params"] }
        },
        {
          "if": { "properties": { "kind": { "const": "standard" } } },
          "then": { "required": ["interpretation"] }
        }
      ],
      "additionalProperties": false
    }
  }
}
