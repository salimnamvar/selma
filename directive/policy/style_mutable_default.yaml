# Policy doctrine — governance intent, guidance, examples, reasoning ONLY.
# MUST NOT be used for linting/evaluation at runtime.
# Machine lint source: matching file under directive/rule/*.json
# Schema contract: schema/policy_doctrine.yaml
# Normative source: .nasim/rules/code/safe-coding.md

doctrine:
  name: style-2-style_mutable_default
  version: 8.2.4
  description: 'Human governance doctrine for STYLE-2: Mutable default argument in function signature.
    Guidance and reasoning only — not machine-executable.'
  spec_version: 8.2.4
  schema_version: 8.2.4
  schema_id: universal-rule-schema
  machine_id: STYLE-2
  paired_rule_file: directive/rule/style_mutable_default.json
  purpose_layer: governance_intent_guidance_examples_reasoning
  runtime_prohibition: This file MUST NOT be read during inspection, evaluation, finding FSM transitions,
    or conflict resolution at runtime. Linting sources are ONLY the JSON rules under directive/rule/.
cross_layer_binding:
  normative_source: .nasim/rules/code/safe-coding.md
  policy_purpose: Governance intent, examples, reasoning, and human guidance
  schema_purpose: Structural projection of machine-evaluable invariants
  lint_source: directive/rule/style_mutable_default.json
  enforcement: Policy influences humans and authoring tools only. Compile-time/runtime evaluation uses
    JSON rules exclusively.
contamination_guard:
  note: 'This document MUST NOT contain machine-executable logic. Prohibited: parameters, conditions,
    evaluator_hint, evaluator_type, evaluator_config, weight, depends_on, conflicts_with, status, created_at,
    expires_at, remediation (as executable carrier), target, lineage.'
  allowed_machine_references:
  - Machine ID STYLE-2 (cross-reference to rule.lineage_id only)
preamble:
  title: Preamble
  purpose: Establish human-readable obligations for STYLE-2 (Mutable default argument in function signature)
    so authors, reviewers, and AI agents can reason about intent without reading evaluator configuration.
  scope: Project style conventions complementary to the Safe Coding Doctrine.
  existential_need: Mutable default arguments are shared across all calls. The default object is created
    once at function definition time, not per call, causing subtle bugs when the mutable is modified.
governance:
  title: Governance & Amendment
  authority: Safe Coding Doctrine / Architecture Owner. Amendments require PR review and must keep policy
    identical in intent to the paired machine rule.
  amendment_process:
  - Propose change via PR touching both this policy YAML and the paired rule JSON when semantics change.
  - Policy-only changes (examples, wording) are allowed if Machine ID intent is unchanged.
  - Never add evaluator_type/evaluator_config or other prohibited fields to this file.
  versioning: MAJOR.MINOR.PATCH aligned with policy_contract_version 8.x
  identity:
    machine_id: STYLE-2
    maps_to: rule.lineage_id
    paired_execution_id_note: Execution ID in schema layer is STYLE-2-MUTABLE-DEFAULT; not redefined as
      executable data here.
definitions:
  title: Definitions
  terms:
  - term: Machine ID
    definition: Immutable lineage identifier STYLE-2 for this directive.
    exclusion: Not an execution ID and not a runtime lookup key for evaluation.
  - term: Paired Rule
    definition: Machine-evaluable document at directive/rule/style_mutable_default.json.
    exclusion: This policy YAML is not a lint source.
  - term: Mutable default argument in function signature
    definition: Mutable default argument in function signature
    exclusion: Not optional style preference when deontic type is Obligation or Prohibition.
principles:
  title: Foundational Principles
  description: This directive supports clarity and consistency complementary to Safe Coding.
  items:
  - id: STYLE
    title: Explicit Over Implicit
    description: Style conventions make contracts and review expectations visible.
directives:
  title: Directives (Rules & Standards)
  specific_directives:
  - type: Prohibition
    description: Mutable default argument in function signature
    machine_id: STYLE-2
    context_conditions: See rule JSON exemptions if any.
    title: Mutable default argument in function signature
  flexible_standards: []
sanctions:
  title: Sanctions & Remedies
  rows:
  - violation_context: Mutable default argument in function signature
    enforcement_action: Machine lint finding via style_mutable_default.json when automatically enforceable;
      otherwise See paired machine rule.
    remediation_path: 'Use None as default and create the mutable inside: def func(a_items=None): items
      = a_items or []'
guidance:
  title: Guidance, Examples, and Reasoning
  purpose: For human and AI reasoning only. Do not execute this section as a linter.
  reasoning: Mutable default arguments are shared across all calls. The default object is created once
    at function definition time, not per call, causing subtle bugs when the mutable is modified.
  explanation: The correct version uses None as default and creates a new list inside. The incorrect version
    uses a mutable default ([]) which is shared across all calls.
  correct_example: |-
    def process(a_items: list[str] | None = None) -> Result[int]:
        items = a_items if a_items is not None else []
        return Result.success(len(items))
  incorrect_example: |-
    def process(a_items: list[str] = []) -> Result[int]:
        return Result.success(len(a_items))
  related_machine_ids: []
  exceptions: See rule JSON exemptions if any.
references:
  title: References & Annexes
  source_doctrine: .nasim/rules/code/safe-coding.md
  policy_schema: schema/policy_doctrine.yaml
  rule_schema: schema/rule_schema.json
  paired_rule: directive/rule/style_mutable_default.json
  anchor_ref: section:directives/style_mutable_default
  related: []
